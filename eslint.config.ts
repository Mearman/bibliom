// For more information, see https://github.com/storybookjs/eslint-plugin-storybook#configuration-flat-config-format
import storybook from "eslint-plugin-storybook";

// Main ESLint configuration for BibGraph monorepo
// This extends the base configuration for workspace-level linting
import baseConfig from "./eslint.config.base.js";

export default baseConfig;
